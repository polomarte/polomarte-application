<div id="wrapper">
  <div id="sidebar-wrapper">
    <ul class="sidebar-nav">
      <li class="sidebar-brand"><%= link_to "Projects", root_path %></li>
        <% @projects.each do |project| %>
          <li><%= link_to project.name.titleize, project_path(project.id)%></li>
        <% end %>
    </ul>
  </div>
  <div id="page-content-wrapper">
    <%= render 'content' %>
  </div>      
</div>